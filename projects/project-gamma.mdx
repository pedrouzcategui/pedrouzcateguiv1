export const metadata = {
  id: "project-gamma",
  slug: "project-gamma",
  title: "Project Gamma",
  date: "2023-07-30",
  excerpt: "A small utility library example, packaged as an MDX showcase.",
  tech: ["Node.js", "Library"],
};

# Project Gamma

Project Gamma is a small utility library (imaginary) that demonstrates how
you can document libraries inside an MDX project file. Include usage examples
and API surface as needed.

```js
// example usage
import { useful } from "project-gamma";
```
